# 地理設施調查 v3.7 網頁 — 啟動說明書

本說明書說明如何從零啟動「地理設施調查」網頁（輸入座標或地址，查詢範圍內生活機能與嫌惡設施）。

---

## 一、事前準備

### 1. 環境需求

| 項目 | 說明 |
|------|------|
| Python | 3.8 以上（建議 3.10+） |
| goplaces | 指令列工具，用於呼叫 Google Places API |
| API 金鑰 | 沿用 OpenClaw，無需另外申請 |

### 2. 安裝 goplaces（若尚未安裝）

```bash
# 方式一：Homebrew（推薦）
brew install steipete/tap/goplaces

# 方式二：Go
go install github.com/steipete/goplaces/cmd/goplaces@latest
```

安裝後在終端機執行 `goplaces --help`，有出現說明即代表成功。

### 3. API 金鑰（沿用 OpenClaw）

金鑰從 **OpenClaw** 設定檔讀取，不需手動設定環境變數：

- 路徑：`~/.openclaw/openclaw.json`
- 欄位：`skills.entries.goplaces.apiKey`

網頁後端與 `run.sh` 會自動讀取上述金鑰，無需再 export。

### 4. Gemini 逾時備援（選用）

若查詢超過 3 分鐘會自動改用 **Gemini** 產生簡要說明。需提供 Gemini 用 API Key（Google AI Studio 金鑰）：

- **方式一**：設定環境變數後再啟動  
  `export GOOGLE_API_KEY=你的金鑰` 或 `export GOOGLE_AI_STUDIO_API_KEY=你的金鑰`
- **方式二**：使用現成 .env 檔  
  若專案或本機有 `.env`（例如 `real_estate_app/.env`）且內含 `GOOGLE_API_KEY` 或 `GOOGLE_AI_STUDIO_API_KEY`，後端會自動嘗試讀取（先找 `ENV_FILE` 指定路徑，再試預設路徑）。  
  手動指定：`ENV_FILE=/path/to/your/.env .venv/bin/python app.py`

未設定時，逾時後僅會顯示「查詢逾時」與「Gemini 備援失敗」，不會影響一般查詢。

---

## 二、第一次啟動（建立虛擬環境）

建議使用 Python 虛擬環境，避免與系統 Python 衝突。

### 步驟 1：進入網頁資料夾

```bash
cd 房仲工具/web
```

（若從 workspace 根目錄出發，路徑為：`房仲工具/web`）

### 步驟 2：建立虛擬環境並安裝依賴

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
```

只會做一次，之後啟動不必重複。

### 步驟 3：啟動網頁

```bash
.venv/bin/python app.py
```

或指定埠號（若 5000 被佔用）：

```bash
PORT=5002 .venv/bin/python app.py
```

### 步驟 4：開啟瀏覽器

在瀏覽器網址列輸入：

- **http://127.0.0.1:5000**（未指定 PORT 時）
- **http://127.0.0.1:5002**（若使用 `PORT=5002`）

看到表單頁面即代表啟動成功。

---

## 三、之後每次啟動（已建過 .venv）

```bash
cd 房仲工具/web
.venv/bin/python app.py
```

若 5000 埠被佔用（例如 macOS AirPlay 接收），可改埠號：

```bash
PORT=5002 .venv/bin/python app.py
```

再開啟 **http://127.0.0.1:5002**。

---

## 四、使用 run.sh（可選）

若已安裝 goplaces 且希望從 OpenClaw 讀取 API 金鑰後啟動，可改用：

```bash
cd 房仲工具/web
chmod +x run.sh
./run.sh
```

注意：`run.sh` 預設使用系統的 `python3`，若你的環境需用虛擬環境，請改用手動指令（第二、三節）。

---

## 五、常見問題

| 狀況 | 處理方式 |
|------|----------|
| `ModuleNotFoundError: No module named 'flask'` | 在 `房仲工具/web` 下執行 `.venv/bin/pip install -r requirements.txt`，並用 `.venv/bin/python app.py` 啟動。 |
| `Port 5000 is in use` | 改用 `PORT=5002 .venv/bin/python app.py`，並開啟 http://127.0.0.1:5002。 |
| `無法解析地址` | 請改輸入「緯度、經度」，或確認地址為完整台灣地址。 |
| 查詢很久 | 依 v3.7 類別數量，單次查詢約 1–2 分鐘屬正常。逾時後會自動改用 Gemini 取得簡要說明（需設定 API Key）。 |
| Gemini 備援失敗 | 請確認已設定 `GOOGLE_API_KEY` 或 `GOOGLE_AI_STUDIO_API_KEY`，或設定 `ENV_FILE` 指向含該 key 的 .env。 |

---

## 六、關閉網頁

在執行 `app.py` 的那個終端機視窗按 **Ctrl + C** 即可停止伺服器。

---

**版本：** v3.7 網頁  
**更新日期：** 2026-02-23
